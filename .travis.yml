language: go
go:
  - 1.7
  - 1.6
sudo: true
addons:
  apt:
    packages:
      - gcc-4.6
      - libcurl4-openssl-dev
      - libjansson-dev
      - linux-libc-dev
      - libtool
      - make
      - cmake
      - autoconf
      - git
      - bzip2
script:
  - cd cli && make test; make cover
  - cd nss && sudo make test; sudo make clean
after_script:
  - goveralls -coverprofile=coverage.out
notifications:
  slack: linyows:2oeZ2qcLnaMjbnEYodwEXKTr
